# fastapi-react-practice

## Instalacion de dependencias
pip install -r requirements.txt 

## Levantar el server mediante Uvicorn
uvicorn backend.main:app --reload

Ctrl + c para cerrar el servidor una vez ejecutandose.
